import styles from './AmountCounter.module.scss';

function AmountCounter(): JSX.Element {
  return <div className={styles.amount_counter}>Total Amount Counter</div>;
}

export default AmountCounter;
