import styles from './HeaderNavProfile.module.scss';

function HeaderNavProfile(): JSX.Element {
  return <div className={styles.header__nav_profile}>Header Nav Profile</div>;
}

export default HeaderNavProfile;
